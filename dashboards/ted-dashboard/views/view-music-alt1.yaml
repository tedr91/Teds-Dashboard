# View: Music-Alt1 — TEMPORARY placeholder. path: music-alt1.
# Exists so the navbar View Launcher auto-combines the "music*" views (music + music-alt1)
# into one "Music" group button, exactly like the home* and calendar* groups (grouping is
# generic: views are grouped by their path prefix up to the first "-"). Content is a
# throwaway placeholder — replace or remove later.
type: custom:grid-layout
path: music-alt1
title: Music-Alt1
icon: streamline-ultimate-color:music-note-1
layout: !include ../shared/layout-content.yaml
cards:
  - type: custom:ted-messagebox-card
    view_layout:
      grid-area: content
    severity: info
    icon: mdi:music
    title: Music-Alt1
    message: Temporary placeholder view.
  - !include ../shared/navbar.yaml
  - !include ../shared/background.yaml
