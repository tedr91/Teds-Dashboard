# kiosk_mode body (consumed by the kiosk-mode HACS plugin via the dashboard config).
# Included by ted-dashboard.yaml as: kiosk_mode: !include ted-dashboard/kiosk.yaml
user_settings:
  - users:
      - wallpanel
    kiosk: true
